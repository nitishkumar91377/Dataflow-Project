# Dataflow-Project
This project processes source data using a dataflow pipeline. Required columns are extracted and aggregate functions such as SUM, COUNT, and AVG are applied with GROUP BY to generate summarized insights. The processed output is then stored in Azure Data Lake Storage for further analytics and reporting.
